---
'dispersa': minor
'create-dispersa': minor
---

Improved the path based lint rule. Optional segments are now flagged via `{segment}?`. Further, support for logical OR was added for segments like `{segmentA|segmentB}`
