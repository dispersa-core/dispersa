{
  "name": "dispersa-examples-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:update": "vitest run --update"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
